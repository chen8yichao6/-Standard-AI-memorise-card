# 项目规则（AGENTS.md · 权威版）

> **启动必读**：本项目规则全文见下方。每次收到用户的「今日任务清单」，第一个动作是**复述本文件的约束**（特别是第一章任务边界、第二章交互式推进、第五章 Git 提交），再动手。
> 不要假设自己记得规则——每次重新读一遍。
> 本文件是项目规则的**权威版**；`CODEBUDDY.md` 是 WorkBuddy 自动读取的**工作区常驻副本**，两者内容保持一致。

---

## 一、任务边界（最重要）

1. 收到我的任务清单后，**这份清单就是今天唯一的任务范围**，只做清单里写的事。
2. 不做清单以外的工作：不建多余的文件、不写清单没提的功能、不顺手优化已有代码。你觉得可以改得更好，先跟我说一句，等我点头。
3. 不提前做以后的天数。只有我把某天的清单发给你、并明确说「现在开始做 Day X」，才允许动那天。
4. 拿不准是否在范围内，先问我，不要猜。

## 二、交互式推进

1. 收到清单后，先只给我今天的执行计划：按什么顺序做、每步怎么做、哪些要我自己动手。**写完停下，等我确认。**
2. 确认后**一次只做一个步骤**，绝不连着往下做。
3. 每步做完立刻停下，告诉我三件事：做了什么、改了哪些文件、怎么验证（附我能亲眼确认的结果）。
4. 然后**停下等我回复「进入下一板块」**。我没说这四个字，你就不要继续。
5. 「进入下一板块」只对当前这一步有效，不代表可以一路做到收尾。

## 三、人机分工

1. 需要我人工操作的（注册账号、点按钮、装软件、截图等）：不要替我做，也不要假装已完成。只告诉我：这步在干什么、一二三步怎么做、做完我该看到什么、容易踩什么坑，然后停下等我。
2. 需要你生成的内容（写文档、建文件、写代码）：**等我说了再做**，不要在我刚说「开始吧」时就把整天的东西生成出来。
3. 需要拍板的地方（选方案、起名字、定风格），把选项和各自代价列给我选，不要自己决定。
4. 验证结果要我能亲眼确认（页面能打开、文件能看到、命令有输出），不接受「已经完成了」这种口头结论。

## 四、讲清楚环节（我是来学的）

1. 每个步骤开始前，先告诉我：它在整个作品里是什么位置、为什么现在做、做完会变成什么样。
2. 每步用「做了什么 → 结果 → 为什么」讲明白，不要只给结果。
3. 我不懂的概念（命令、报错、术语、配置），一两句话解释清楚，不要默认我会。
4. 报错时先告诉我错在哪、可能什么原因、打算怎么修，我同意后再动手。不要输出教程式长文。

## 五、Git 与提交

1. 我授权你代为执行 git 提交和推送，但**提交前必须先列出**本次会改动哪些文件、每个文件属于哪天的任务，等我确认后再提交。
2. commit 信息格式：`Day X｜一句话说明`。
3. 密钥、`.env`、数据库连接串永远不进代码、不进提交。
4. 一天的任务全部做完、我核对完文件清单之后才提交，中途不要边做边提交。

## 六、当日收尾

1. 我说「今天做完了」之后，请你逐条对照清单里的「完成标准」，输出一张表：完成标准 / 状态（已完成·部分完成·未完成）/ 证据（文件、页面、命令输出）。
2. 核对必须诚实，没做完、做了一半、跳过没做的，直接标出来并说明原因，不要为了凑「全部完成」而模糊处理。
3. 有未完成项时，告诉我还差什么、接不接着做，**由我决定**，不要自己往下做。
4. 全部完成时，按清单的「收尾指令」整理当天成果，提醒我该交哪张截图、该在群里回什么。

## 七、出问题的时候

1. 我要求撤销时，用安全回退（`git revert`，保留历史），禁止 `git reset --hard` 和强制推送。
2. 回退前先和我确认恢复到哪个提交，回退后列出当前状态让我核对。
3. 同一个操作反复失败时停下来报告，说清楚试了什么、报什么错、你判断的原因，不要循环重试。

---

## 八、技术规范（Day 5 技术设计定稿）

> 本章是「保存项目规则」这一步的产出：把 `TECH_DESIGN.md`（技术总纲，v0.4）里定稿的**硬规则**浓缩到此，供第 5 步「小步开发和迭代」起每次开发遵循。这里只存「必须遵守、不许违反」的底线，细节一律以 `TECH_DESIGN.md` 为准。

### 8.1 技术栈（已定稿，不随意更换）

- **客户端**：Flutter（复用 M1 工程）+ flutter_riverpod + go_router + record + just_audio + sqflite + path_provider + dio + flutter_secure_storage + connectivity_plus + uuid。
- **服务端**：Python 3.13 / FastAPI + SQLAlchemy 2.0 + Alembic + Pydantic v2 + PostgreSQL（托管）+ Uvicorn（开发期本机直跑，免 Docker）。
- **鉴权**：JWT 双令牌（Access + Refresh）。
- **音频存储**：对象存储（本机磁盘仅离线开发期用）。

### 8.2 项目结构（目录 + 分层依赖）

- 目录：`app/` 客户端、`server/` 服务端、`docs/` 文档、`TECH_DESIGN.md` 技术总纲、`PRD.md` 需求。
- **分层依赖（硬规则）**：客户端 `ui → state → sync → data → models → core`；服务端 `api → service → repository → models → core`。
- **只能向下依赖，禁止反向依赖**；UI 不直接碰数据源（sqflite / HTTP / 文件路径）；models 无外部依赖；服务端路由不写业务逻辑。

### 8.3 数据模型（8 条硬约定）

1. 主键稳定唯一，不依赖自增序号。
2. 所有业务数据带 `user_id`，查询强制按当前用户过滤。
3. `created_at` / `updated_at` 成对存在，自动生成。
4. **软删除 = 墓碑 + 音频文件解耦**：删除时写 `is_deleted=true` + `deleted_at`（墓碑），音频文件移入 `trash/` 暂存区；墓碑同步成功后才物理删文件。**只有墓碑依赖网络，音频文件不依赖。**
5. `db_version` 存在且可读。
6. 可空字段必须有默认值。
7. 需上云实体必须带 `sync_status` + `updated_at`。
8. `memory_id` / `tag_id` 由**客户端生成 UUID**，服务端沿用同一主键（幂等重试的前提）。

### 8.4 同步机制（4 条规则）

1. **幂等 upsert**：`PUT /memories/{id}`，id 由客户端 UUID 生成（重试不产生重复条目）。
2. **删除靠墓碑**：删的是 `deleted_at`，不是物理行。
3. **增量游标**：`pull_cursor = change_log.seq`，只拉变化量。
4. **冲突**：较新者胜 + 保留冲突副本（比 `updated_at`）。

### 8.5 错误处理（3 条禁令）

1. 禁止静默失败（catch 后必须上报 / 改状态 / 展示）。
2. 禁止吞掉同步失败（必须体现在 `failed_count` 与同步状态视图）。
3. 禁止把技术错误直接给用户（翻译成「发生了什么 + 我能做什么」）。

### 8.6 密钥与配置（红线）

- `.env` / `*.key` / `*.pem` 永不提交；只提交 `.env.example`（键名 + 假值）。
- 仓库不得出现真实连接串 / 密钥 / 账号密码。
- 密钥不入镜像、不入库，只从环境变量读。
- 提交前自检：`git diff --cached` 搜 `password` / `secret` / `postgresql://`。

### 8.7 迁移（3 条铁律）

1. 只加不删（加列安全，删列 / 改类型不可逆）。
2. 先备份再升级。
3. 迁移脚本与代码同一次提交。

### 8.8 安全红线

- 客户端与服务端全程 HTTPS / TLS，不接受明文 HTTP。
- 凭据只存系统安全存储（不落普通库 / 明文文件）。
- 密码：客户端不存明文，服务端只存加盐哈希（Argon2id 或 bcrypt）。

---

## 九、改完必验（Day 6 新增）

> 本章是 Day 6「检查并完善规则文件」追加的**自己的规则**，防的是本项目反复踩过的一个坑：**静默失败与编辑竞态覆盖**——「以为改好了，实际旧内容还在、或部分改动被互相覆盖丢了，表面却看不出」。

1. **改完必须独立验证落盘**。任何对文档、代码、配置的修改完成后，都要用与「写」无关的独立手段确认改动真实生效：用关键词搜索确认新表述已写入、旧表述已清除，用 `git diff` 或实测命令确认结果。不能只凭「我改过了」下结论。
2. **同一文件的多次编辑必须串行**。禁止对同一个文件并行发起多处修改（并行 Edit 会互相覆盖，造成「只生效了一部分」）。改完一处、验证一处，再改下一处。
3. **文档与现实不一致时，以现实为准并回改文档**。设计文档里写的环境、版本、命令，若与本机实测不符，以实测为准并回改文档，禁止让文档「嘴硬」。

---

## 十、个人习惯（Day 6 追加）

> 本章基于项目实际用途（学习记录 / 开会记录 / 灵感记录 / 口语练习 / 决策辅助）追加，落在前九章没覆盖的空档上，管的是「我（AI）的工作习惯」。

1. **临时文件归位**。项目根目录只放正式交付物（文档 / 工程 / 配置）；AI 产生的临时文件（调试脚本、diff 导出、状态快照）一律放 `.workbuddy/memory/` 或系统临时目录，用完即删，不进 git、不污染根目录。
2. **语言边界**。文档、对话回复、commit 说明默认简体中文；代码本身（变量名 / 函数名 / 注释）用英文；技术术语首次出现给「中文 + 英文原文」（如墓碑 tombstone、幂等 idempotent）。
3. **结论可溯源**。AI 给出的结论、建议、判断必须标明依据来源（来自哪个文档哪一节 / 哪次实测 / 哪条数据），并明确区分「有据可查的事实」和「我的推断 / 猜测」；推断要明说是推断，不能包装成事实。

---

## 附：AI 执行说明

以上十章是规则正文。以下是配合方式的具体落地说明，用于消除歧义。

1. **「一次只做一个步骤」的粒度**：以执行计划里的**编号步骤**为准。用户说「进入下一板块」即推进下一步；若要按清单任务板块推进，由用户另行说明。
2. **计划与执行分离**：收到清单后只输出执行计划，不产出任何交付物；等用户明确确认后才开始第一步。
3. **每步汇报格式**：做了什么 → 改了哪些文件 → 怎么验证（给用户可亲眼确认的证据：文件路径、命令输出、页面）。
4. **人工操作边界**：凡涉及注册、点击、安装、截图等人工动作，只提供操作指引（这步在干什么 / 一二三步 / 做完该看到什么 / 常见坑），不代做、不假装完成。
5. **验证标准**：以可观察结果为准，不使用「已完成」这类口头结论。
6. **Git 操作**：提交前先列改动文件清单及归属日期，获确认后提交；仅用 `git revert` 回退，禁止 `git reset --hard` 与强制推送；密钥类信息不入库。
