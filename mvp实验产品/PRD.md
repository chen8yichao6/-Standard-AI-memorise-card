# AI 记忆卡 · 产品需求文档（PRD）

> **项目名称**：AI 记忆卡（AI Memory Card）
> **文档类型**：产品需求文档（Product Requirements Document）
> **版本**：v0.5（2026-09-20 第四轮修订：**MVP 范围加入后端与云同步** —— 账号改服务端、记忆上云多端同步；同步调整 1.3 / 4.1 / 4.2 C7 / 6 / 7 / 8，新增 F-07 云同步、P-12 同步状态视图、T-7 同步状态表、K 组验收；本轮变更记录见附录 C.6）
> **编制日期**：2026-09-20
> **当前阶段**：Vibe Coding 五步工作流 · 第 2 步（写 PRD）
> **关联文档**：`research.md`（需求研究，第 1 步产物）、`录音卡产品-软件层SRS-需求规格说明书.md`（口语线详规）
> **文档边界**：本 PRD 只描述「做什么、做到什么程度、怎么算做完」；**不涉及技术选型**（Day 5 另行产出）、**不涉及代码实现**。

---

## 1. 产品概述

### 1.1 一句话定位

**AI 记忆卡是一款「说出来就记得住」的随身记录工具** —— 让大学生和职场人士把随时冒出的想法、听到的内容、要办的事，用「按住说话」或一句话速记的方式存下来，在需要的时候随时找回来。

### 1.2 产品形态与范围边界

AI 记忆卡是**软硬一体**产品（硬件录音卡 + 软件层），但**本项目只负责软件层**。软件层自 v0.5 起由两部分组成：**客户端 App（Flutter）+ 服务端（Python / FastAPI）**。

产品有两条能力线：

| 线 | 一句话 | 本期状态 |
|---|---|---|
| ① 日常记录 | 随时记录 → 自动整理入库 → 辅助决策 | **本期主体**（MVP 做「本地优先 + 云端同步」的记录闭环） |
| ② 口语练习 | 复读 / 跟读 / 断句 / 变速 / 自评 | 已有 M1 复读机引擎，本期**不重做、不验收**；记忆与口语的打通**本期不做** |

> **定位来源**：竞品研究（`research.md` 第 5 节）显示，「卡片式 AI 录音器」已是跑通的商业品类，且「记录 → 整理 → 决策」正是其核心卖点。因此产品定位从早期的「英语口语录音卡」升级为「AI 记忆卡」，口语练习成为「记忆」能力的一个特例而非全部。

### 1.3 本期（MVP）目标

**用最小成本验证一件事：用户愿不愿意用「张嘴说话」替代「打字记录」。**

MVP 交付一个**「本地优先 + 云端同步」的记录闭环**：记录先落本机，**离线也能记**；联网后自动同步到服务端，换设备登录后能看到同一套记忆。

```
打开 App → 登录（服务端账号）→ 按住说话（或速记一行字）→ 先存本机
        → 在列表里看得到 → 点进去能回听/看文字 → 能改标题、能删
        → 联网后自动上云 → 换设备登录，记忆仍在
```

**本期成功的判断标准**：一个真实用户在不看说明书的情况下，能完成上述闭环，并且第二天还愿意打开它记第二条；**且在无网环境下，记录动作不被阻塞**。

### 1.4 名词定义

| 名词 | 含义 |
|---|---|
| **记忆条（Memory）** | 产品的最小内容单元。一条记录 = 一段录音 + 标题（+ 可选文字说明 / 标签）。 |
| **快速记录** | 首页主入口。两种形态：① 按住说话（录音）② 速记（直接打字）。 |
| **记忆列表** | 首页主体，按时间倒序排列全部记忆条。 |
| **延后（后续功能）** | 需求成立、但**本期不实现也不验收**的功能；文档中保留，其是否做、何时做需**另行立项**，不属本 PRD 的承诺范围。 |
| **砍掉（本期不做）** | 主动排除的功能，**本 PRD 有效期内明确不做**，并逐条记录砍掉理由（见 4.2）。 |
| **本地优先（offline-first）** | 记录的**主副本先落本机**，界面立刻可用；联网后再同步到服务端。**无网不阻断任何记录动作**。 |
| **云同步** | 把本机记忆（元数据 + 音频）与服务端对齐的双向过程，使本机与服务端最终一致。 |
| **同步状态** | 单条记忆的云同步进度：`local_only`（仅本机）/ `pending`（待上传）/ `synced`（已同步）/ `failed`（同步失败）。 |

---

## 2. 目标用户

### 2.1 用户画像

**聚焦两类人**：大学生 + 职场人士。

| 用户 | 画像 | 核心诉求 |
|---|---|---|
| **A. 长期记录学习者** | 大学生为主。上课、听讲座、读书、自学网课，信息量大且碎。 | 当下不方便打字，但事后需要能翻回来；不想让想法「说完就没了」。 |
| **B. 口语练习者** | 大学生 + 职场人士。需要跟读、纠音、反复练的人（含考研、雅思、职场英语）。 | 随时能开练；练习素材最好来自自己的真实内容。 |

> 早期会议口径中的「中小学生 + 老师看学情」**本期不做**（理由：与本期「大学生 + 职场人士」的定位分散；「老师看学情」还需机构侧多账号能力，不在本期范围）。

### 2.2 典型使用场景（MVP 覆盖范围内）

| # | 场景 | 用户动作 | 期望结果 | 本期是否覆盖 |
|---|---|---|---|---|
| S1 | 下课路上突然想到一个论文点子 | 掏出手机按住说话 10 秒 | 存成一条记忆，晚上能翻到 | ✅ 覆盖 |
| S2 | 上课录音，课后想回听重点 | 按住说话录一整节 | 录音完整保存，可回听、可拖动进度 | ✅ 覆盖 |
| S3 | 读书时看到一句好话 | 速记一行字 | 存成文字记忆，列表可读 | ✅ 覆盖 |
| S4 | 想给记录分类 | 编辑标题 / 加标签 | 后续能按标签筛选 | ✅ 覆盖 |
| S5 | 忘了某条记录说了什么 | 打开列表逐条回听 | 找得到 | ✅ 覆盖（人工翻找） |
| S6 | 想直接问「我上周记的那个点子是什么」 | 用一句话检索 | 系统直接给答案 | ❌ **本期不做**（无转写与检索能力，见 4.1） |
| S7 | 想把某条记录拿来练跟读 | 一键转成跟读练习 | 进入复读练习 | ❌ **本期不做**（需与 M1 引擎对接，见 4.1） |

### 2.3 用户需求 → 功能映射

| 用户诉求 | 对应功能 | 优先级 |
|---|---|---|
| 「我的记录不能被人随便看」 | F-01 用户登录 | P0 |
| 「想到就要马上记下来」 | F-02 快速记录 | P0 |
| 「记录多了要能翻得到」 | F-03 记忆列表 | P0 |
| 「一条记录要能看细节、能回听」 | F-04 记忆详情 | P0 |
| 「标题写错了、想加个标签」 | F-05 基础管理 | P0 |
| 「想知道占了多少空间、能不能清」 | F-06 设置 | P2 |
| 「换手机 / 换电脑后，我的记录还在」 | F-07 云同步 | P0 |

---

## 3. MVP 功能

### 3.1 功能总览

| 编号 | 功能 | 优先级 | 一句话说明 |
|---|---|---|---|
| F-01 | 用户登录 | P0 | **服务端**账号注册 / 登录 / 退出，隔离每个人的记录 |
| F-02 | 快速记录 | P0 | 按住说话录音，或直接速记一行文字 |
| F-03 | 记忆列表 | P0 | 倒序展示全部记忆条，可看标题、类型、时长、时间 |
| F-04 | 记忆详情 | P0 | 查看单条记忆：回听录音 / 看文字，看创建时间 |
| F-05 | 基础管理 | P0 | 编辑标题与标签、删除记忆、删除二次确认（其中**标签子功能为 P1**） |
| F-06 | 设置 | P2 | 查看本地占用空间、**同步状态**、账号退出、关于信息 |
| F-07 | 云同步 | P0 | 记忆上云、多端拉取、离线队列与失败重试 |

> **命名说明**：功能编号 `F-xx`、页面编号 `P-xx`、验收条 `AC-xx`、数据表 `T-x`，全文档保持一致，便于自检与评审对齐。

---

### 3.2 F-01｜用户登录（P0）

**用户故事**：作为一个有私人记录需求的用户，我希望我的记录只属于我，这样我不用担心别人翻我的手机时看到内容，换设备也能拿回自己的记录。

**涉及页面**：P-02 登录页、P-03 注册页、P-09 设置页（退出登录入口）

**用户动作**

1. 首次打开 App，进入登录页。
2. 选择「注册」→ 输入账号（用户名，4–20 位）+ 密码（6–20 位）→ 可选填写昵称 → 提交（调用服务端注册接口）。
3. 已有账号时输入账号 + 密码 → 登录（调用服务端登录接口）。
4. 在设置页点击「退出登录」。

**可观察结果**

- 注册成功后，**服务端创建账号**，客户端拿到登录凭据并直接进入首页，列表为空并显示空状态引导文案。
- 登录成功后进入首页，只能看到**当前账号**的记忆条；换账号登录，看到的是另一套数据。
- 退出登录后回到登录页；再次打开 App 保持退出状态。
- 账号已存在时注册 → 明确提示「该账号已存在」，不创建重复账号。
- 密码错误 → 提示「账号或密码不正确」，不提示具体是哪一个错（避免被人试出账号是否存在）。
- **离线时无法注册 / 登录新账号**，但**已登录过的账号可离线继续使用**（凭本机保存的登录态进入本地数据，见 F-07）。

**边界说明**

- 本期账号为**服务端账号**：注册与登录均走服务端接口；密码**只在服务端做加盐哈希存储**，**客户端不保存明文密码**。
- 本期**不做**：短信验证码、第三方登录、找回密码（这三项见 4.2 C7）。
- 登录凭据（Access / Refresh Token）保存在**系统安全存储**中，用于免登录与云同步鉴权；续期规则见异常 E15。

---

### 3.3 F-02｜快速记录（P0）

**用户故事**：作为一个走在路上突然有想法的人，我希望不用打字就能记下来，这样想法不会丢。

**涉及页面**：P-04 首页（主按钮 + 速记入口）、P-05 录音进行中视图、P-08 速记输入视图

**用户动作**

1. 在首页点击并**按住**主按钮 → 开始录音；松开手指 → 结束并保存。
2. 录音过程中可上滑取消（不保存）。
3. 在首页点击「速记」→ 弹出输入框 → 输入一行文字 → 保存。
4. 保存后自动生成默认标题（如「09-20 20:31 的记录」），进入列表顶部。

**可观察结果**

- 按住按钮的**全过程**有明确视觉反馈：录音中显示计时、波形或呼吸动画，用户能一眼确认「正在录」。
- 松开后 1 秒内，新记忆条出现在列表**最上方**，类型标识正确（录音 / 文字）。
- 录音文件成功落盘；杀掉 App 再打开，录音仍能正常回放（说明已持久化，不是内存态）。
- 上滑取消后，列表**不新增**任何条目，且不残留空文件。
- 单条录音**最短 1 秒**（短于 1 秒视为误触，提示「说话时间太短，已取消」）；**最长 30 分钟**（到点自动结束并保存，并提示）。

**边界说明**

- 本期**不做实时转写**，录音保存后**没有自动文字**，用户可手动补一句文字说明（见 F-05）。
- 被中断的录音（来电 / 切后台）**保留已录部分**，以 `record_status = incomplete` 保存，界面标注「未完成」（见 7.1 E2）。
- 本期不做后台录音、不做通话录音、不做多麦克风降噪。

---

### 3.4 F-03｜记忆列表（P0）

**用户故事**：作为一个记了几十条内容的人，我希望一眼看到我记过什么，这样我能快速找到想找的那条。

**涉及页面**：P-04 首页 / 记忆列表页、P-10 空状态视图（列表为空时内嵌）

**用户动作**

1. 进入首页，看到记忆条列表。
2. 上下滚动浏览。
3. 点击任意一条 → 进入详情页。
4. 长按任意一条 → 弹出快捷操作（重命名 / 删除）。
5. 点击列表中录音条的播放按钮 → 就地试听（不进入详情页）。

**可观察结果**

- 列表按**创建时间倒序**排列（最新的在最上面）。
- 每条至少显示：**标题、类型图标（录音/文字）、时长（录音条）、创建时间**；被中断的录音额外显示「未完成」标识。
- 列表条数 ≥ 100 条时，滚动流畅无明显卡顿（验收见 AC-D7）。
- 长按菜单里的「删除」执行后，该条从列表消失；重启 App 后依然不存在。
- 列表为空时显示空状态（见 7.2），而不是一片空白。
- 就地试听时，同一时刻**只有一条**在播放；播放另一条会自动停止前一条。

**边界说明**

- 本期**不做全文搜索**（依赖转写，本期无转写能力）、**不做标签筛选**、**不做排序切换**（默认时间倒序已覆盖核心场景）。
- 默认标题仅作占位，用户可自行修改。

---

### 3.5 F-04｜记忆详情（P0）

**用户故事**：作为一个回听录音的人，我希望看到这条记录的完整信息并能随手回听，这样我能确认自己记了什么。

**涉及页面**：P-06 记忆详情页、P-11 二次确认弹窗（详情页内删除时）

**用户动作**

1. 从列表点进任意一条记忆。
2. 播放 / 暂停录音，拖动进度条定位。
3. 查看创建时间、时长、文字说明、标签。
4. 点「编辑」修改标题与文字说明。

**可观察结果**

- 详情页展示：标题、类型、创建时间、时长（录音）、完整文字内容、标签。
- 录音可**播放 / 暂停 / 拖动进度**；进度条与当前时间同步变化。
- 播放到结尾自动停止，按钮状态回到「播放」。
- 编辑标题并保存后，**返回列表时列表里的标题同步更新**（不是要刷新才变）。
- 音频文件缺失时，页面不崩溃，显示可读的异常提示（见 7.1）。

**边界说明**

- 本期详情页无转写文本区（本期无转写能力）；文字内容仅为用户手动输入。
- 不做分享、导出、多选批量操作。

---

### 3.6 F-05｜基础管理（P0；其中标签子功能为 P1）

**用户故事**：作为一个记了几十条内容的人，我希望整理和清理我记的东西，这样列表不会越来越乱。

**涉及页面**：P-07 记忆编辑视图（改标题 / 打标签）、P-06 记忆详情页（删除入口）、P-04 首页（长按菜单入口）、P-11 二次确认弹窗

**用户动作**

1. 在详情页或列表长按菜单中，修改标题。
2. 给记忆条添加 / 移除标签（可新建标签）。
3. 删除单条记忆（详情页内或列表长按菜单）。
4. 删除时在二次确认弹窗里点击「确认删除」。

**可观察结果**

- 标题修改后立即生效，列表与详情页保持一致。
- 标签添加后，详情页出现该标签；可移除。
- 删除操作**必须经过二次确认弹窗**，弹窗明确写明「删除后不可恢复」。
- 确认删除后：条目消失、**关联的本地音频文件同时被清理**（不留孤儿文件）。
- 在二次确认弹窗点「取消」，数据与文件均无变化。

**边界说明**

- 本期**不做回收站 / 撤销删除**（删除即永久），因此二次确认是硬要求。
- 不做批量删除、不做标签管理独立页面。

---

### 3.7 F-06｜设置（P2）

**用户故事**：作为一个关心手机存储的人，我希望知道这些记录占了多少空间，并能退出登录。

**涉及页面**：P-09 设置页、P-11 二次确认弹窗（退出登录确认）

**用户动作**

1. 进入设置页，查看「已占用空间」与「记忆条数量」。
2. 查看**同步状态**（最近同步时间、待同步条数），可手动触发一次同步。
3. 点击「退出登录」，在二次确认后退出。

**可观察结果**

- 设置页显示：当前账号名、记忆条总数、本地占用空间（约值，单位 MB）。
- 设置页显示**同步状态**：最近同步时间、`pending` / `failed` 条数；点「立即同步」能触发一次同步并刷新结果。
- 退出登录需二次确认；确认后回到登录页。
- 显示版本号与「关于」信息。

**边界说明**

- 本期只支持**单条删除**：不做批量删除，也不做一键清空全部数据。
- 不做主题切换、不做语言切换、不做通知设置。
- 退出登录**只清除本机登录凭据，不删除本机数据**（避免误操作导致记录丢失）；下次同账号登录后继续使用同一套本地数据。

---

### 3.8 F-07｜云同步（P0）

**用户故事**：作为一个在手机和电脑之间来回用的人，我希望我的记录能自动跟着我走，这样换设备不用重新记一遍。

**涉及页面**：P-04 首页（列表项同步标识）、P-06 记忆详情（条目同步状态）、P-09 设置页（同步状态区）、P-12 同步状态视图

**用户动作**

1. 新建 / 编辑 / 删除一条记忆后，App 在后台自动上传或同步该变更。
2. 联网状态下拉取本账号在其他设备上产生的变更。
3. 在设置页查看同步状态，必要时点「立即同步」手动触发。
4. 无网时照常记录，待网络恢复后由 App 自动补传。

**可观察结果**

- **离线也能记**：断网时按住说话、速记、编辑、删除全部可用，记录落在本机并把同步状态标为 `pending`。
- 网络恢复后一定时间内，本机 `pending` 条目**自动**变为 `synced`，**无需用户手动操作**。
- 在设备 A 新增一条记忆，设备 B 登录同一账号后能拉到该条，内容一致（含音频可回放）。
- 在设备 A 删除一条记忆，设备 B 同步后该条也消失。
- 列表项与详情页能看到该条的**同步状态**；`failed` 条目可手动重试。
- 上传失败给出**可重试**的提示，不产生重复条目（同一记忆不因重试出现多条）。

**边界说明**

- 本期同步策略为**本地优先**：本机永远是可用主副本，云端承担同步与备份职责；**网络不可用不阻断任何记录动作**。
- 音频文件与元数据一起同步：元数据走同步接口，音频走独立上传通道。
- 冲突处理（同一记忆在两台设备同时被改）：本期采用**「按最近修改时间取胜 + 保留冲突副本」**的最简策略，**不做三方合并**（见异常 E16）。
- 本期**不做**：团队共享、协作编辑、跨账号分享、实时协同（同步为准实时，允许秒级延迟）。

---

## 4. 后续功能与本期砍掉的功能

### 4.1 后续功能（本期不实现、不验收）

> **边界声明**：以下功能本期**一律不实现、不纳入验收**。它们是否做、何时做，需在后续版本**另行立项决策**，不属本 PRD 的承诺范围 —— **本 PRD 的验收范围只包含第 3 章的 F-01 ~ F-07。**

| 功能 | 说明 | 为什么延后 |
|---|---|---|
| **AI 接入 · 转写** | 录音保存后自动转成文字（录音后转写，**不做实时**） | 依赖第三方 ASR 供应商，**供应商尚未定**（`research.md` 第 10 节：讯飞 / 腾讯挂起未决）；本期已建好服务端（可承担**密钥托管与调用中转**），但仍需先跑通「记得下、找得到」的基础闭环 |
| **AI 接入 · 智能整理** | 自动生成标题、标签、摘要 | 依赖转写结果；且属于「锦上添花」，不影响闭环成立 |
| **AI 接入 · AI 问答** | 对某条记忆或全部记忆提问（类 Ask Plaud） | 依赖转写 + LLM，成本与工期都超出本期；先用检索验证价值 |
| **内容接入** | 导入已有音频文件、粘贴网页/文档链接 | 需额外的格式解析与内容清洗工作量，且不属于本期「随手记」的核心动作 |
| **知识库接入** | 把记忆整理为可长期积累的个人知识库 | 本期无数据基础（真实记录量为 0，整理效果无法验证），需先积累真实使用数据 |
| **记忆 ↔ 口语打通** | 一条记忆一键转成跟读练习（复用 M1 复读机引擎） | 需要与已有 M1 引擎做接口对接；本期先保证记忆侧数据可用 |
| **找回密码** | 忘记密码后通过短信 / 邮箱重置 | 需要短信服务商或邮件通道，属**账号增强**；本期账号已用「用户名 + 密码」实现闭环，不影响主线 |

### 4.2 本期砍掉的功能及理由 ⭐

> 本节回答当日核心问题：**「你砍掉了什么功能？为什么砍它？」**
> 判定标准见 4.3，每条都写明「砍掉理由」。

| # | 砍掉的功能 | 砍掉理由 |
|---|---|---|
| C1 | **硬件录音卡本体** | 不在本项目职责内（本项目只做软件层）。硬件仍在打样阶段，软件必须先独立跑通，否则软硬互相阻塞。 |
| C2 | **数字人陪练**（虚拟人形象 + 口型同步 + 实时对话） | ① 技术依赖第三方虚拟人服务或自研口型同步，**成本与工期都不是本期量级**；② 与「随时记忆、贴身快速使用」的核心场景无关；③ 用户已明确「拓展的比如数字人选择这种不做」。 |
| C3 | **全要素口语评测**（舌位、口型、语调、音变等） | ① **舌位在手机端物理上不可行**（需超声/EMA 设备），不是成本问题是原理问题；② 剩余维度需要专业评测引擎，超出本期能力；③ 属 M6/P3 阶段。 |
| C4 | **考试题库**（四六级 / 雅思 / 考研口语） | ① 题库内容有版权与持续更新成本；② 与「记录自己的内容」这一主线冲突 —— 我们要的是「记自己的」，不是「背题库的」。 |
| C5 | **实时转写 / 实时对话** | ① 流式 ASR 成本显著高于转写后处理；② 弱网环境下体验崩塌，首发风险大；③ 用户场景是「先记下来，事后看」，不是「边说边看字」。 |
| C6 | **深度知识图谱 / 复杂决策引擎** | ① 「辅助决策」的价值可以用**检索 + AI 问答**这种轻量形态验证，没必要一上来就建图谱；② 建图需要大量结构化数据，而我们**连数据都还没有**。 |
| C7 | **账号增强**（短信验证码 / 第三方登录） | ① 本期账号已实现闭环（用户名 + 密码，服务端校验），这两项属**增强**而非必需；② 短信验证码需短信服务商、第三方登录需 OAuth 对接，**渠道依赖都不在本期量级**；③ 加进来会挤占云同步（F-07）的工时。（说明：原 C7「云账号体系」已于 v0.5 **撤销** —— 账号与云同步改列本期功能，见 F-01 / F-07。） |
| C8 | **iOS 版本** | 团队资源有限，先在一个平台把闭环跑通、把 bug 修干净，再谈第二平台。 |
| C9 | **社交 / 协作 / 分享 / 排行榜 / 游戏化** | ① 产品定位是「贴身私人工具」，社交属性会破坏「私密记录」的信任感；② 团队协作不在目标用户诉求内（用户是个人）。 |
| C10 | **批量操作**（批量删除 / 批量打标签 / 多选） | ① 本期数据量小（个人记录初期通常 < 100 条），批量操作收益低；② 误操作风险高，而本期**没有回收站**兜底。 |

### 4.3 砍掉功能的三条共同判据

任何新增功能提议（开发过程中或之后），都用这三条判据逐个筛：

1. **依赖是否已就绪？** —— 依赖未定（如 ASR 供应商未选）、依赖外部硬件（如录音卡未打样）、依赖物理上做不到的技术（如手机测舌位）→ 砍或延后。
2. **是否属于「随时记忆、贴身快速使用」的核心闭环？** —— 不属于的场景（考试、社交、虚拟人）→ 砍。
3. **28 天内能否完成并验证？** —— 做不完、或做完了也没法验证价值 → 延后，不砍能力但砍本期范围。

> 补充说明：「延后」（4.1）与「砍掉」（4.2）的区别 —— 前者是**排期问题**（能力成立但本期不做，未来是否做需另行立项），后者是**方向问题**（本 PRD 有效期内明确不做）。两者都不是模糊的「以后再说」：4.1 的每一项都写了延后理由，4.2 的每一项都写了砍掉理由。

---

## 5. 页面与交互要求

### 5.1 页面 / 视图列表

| 编号 | 页面 / 视图 | 入口 | 核心内容 | 关键交互 |
|---|---|---|---|---|
| P-01 | 启动 / 欢迎页 | 打开 App | 品牌标识、初始化加载 | 已登录 → 直达 P-04；未登录 → P-02 |
| P-02 | 登录页 | P-01 / 退出登录 | 账号输入、密码输入、登录按钮、去注册 | 提交 → 校验 → 成功进 P-04 |
| P-03 | 注册页 | P-02 | 账号、密码、确认密码、昵称（选填） | 提交 → 校验 → 成功进 P-04 |
| P-04 | **首页 / 记忆列表页** | 登录成功 / P-01 | 记忆条列表 + **按住说话**主按钮 + 速记入口 | 按住录音、松开保存、长按条目出菜单、点击进 P-06 |
| P-05 | 录音进行中视图（覆盖层） | P-04 按住主按钮 | 计时、录音动效、上滑取消提示、松手结束提示 | 松手 → 保存；上滑 → 取消 |
| P-06 | 记忆详情页 | P-04 点击条目 | 标题、类型、时间、时长、播放器、文字内容、标签 | 播放/暂停/拖动、编辑、删除 |
| P-07 | 记忆编辑视图 | P-06「编辑」/ 列表长按「重命名」 | 标题输入、文字说明输入、标签选择与新建 | 保存 → 回退并同步列表 |
| P-08 | 速记输入视图（弹层） | P-04「速记」 | 单行/多行文字输入、保存按钮 | 保存 → 生成文字型记忆条 |
| P-09 | 设置页 | P-04 顶部入口 | 账号名、记忆条数量、占用空间、退出登录、版本信息 | 退出登录（二次确认） |
| P-10 | 空状态视图 | P-04 列表为空时内嵌 | 引导文案 + 图示 + 引导操作按钮 | 点击引导 → 直接开始记录 |
| P-11 | 二次确认弹窗（通用覆盖层） | 删除记忆（P-04 长按 / P-06）/ 退出登录（P-09） | 后果说明文案（含「不可恢复」）+「取消」「确认」按钮 | 确认 → 执行；取消 → 无任何变化 |
| P-12 | 同步状态视图（覆盖层） | P-09 设置页点「同步状态」 | 最近同步时间、`pending` / `failed` 条数与列表、手动重试入口 | 点「立即同步」→ 触发同步并刷新结果 |

> **本期页面 / 视图合计 12 个**（其中 P-05、P-07、P-08、P-10、P-11、P-12 为视图 / 弹层 / 覆盖层，不占独立路由；独立页面为 6 个：P-01、P-02、P-03、P-04、P-06、P-09）。

### 5.2 关键交互流程

**流程 1 · 首次使用**

```
打开 App → P-01 启动页 → 未登录 → P-02 登录页 → 注册 → P-03
        → 注册成功 → P-04 首页（P-10 空状态）→ 看到引导文案 → 按住说话
```

**流程 2 · 核心记录闭环（产品最关键的一条路径）**

```
P-04 按住主按钮 → P-05 录音中（计时 + 动效）→ 松手
   → 保存成功（1 秒内）→ 列表顶部出现新条目 → 点击进 P-06 → 回听 → 编辑标题 → 返回列表 → 标题已更新
```

**流程 3 · 删除**

```
P-04 长按条目 / P-06 点击删除 → P-11 二次确认弹窗（明确写「删除后不可恢复」）
   → 确认 → 条目消失 + 音频文件清理
   → 取消 → 无任何变化
```

**流程 4 · 云同步（v0.5 新增）**

```
记录 / 编辑 / 删除 → 本机立刻生效（同步状态 = pending）
   → 网络可用 → 后台自动上传「元数据 + 音频」→ 服务端落库
   → 状态变 synced（列表项同步标识更新）
   → 其他设备登录同账号 → 拉取变更 → 本地也出现该条

断网 → 记录照常落本机（pending）→ 网络恢复 → 自动补传 → synced
上传失败 → 标 failed + 可重试 → 点重试 → 重新上传（不产生重复条目）
```

### 5.3 交互原则（硬性要求）

1. **一屏一主操作**：首页只有一个视觉最重的主按钮（按住说话），其他入口不得抢视觉焦点。
2. **状态必须有反馈**：录音中、保存中、加载中、播放中，四种状态都必须有可被眼睛看见的反馈，禁止「点了没反应」。
3. **破坏性操作必须二次确认**：所有删除均需确认弹窗，且弹窗文案必须写明后果（不可恢复）。
4. **不打断用户**：除非发生错误，否则不弹打断式弹窗（toast 优先）。
5. **单手可达**：主按钮位置需满足单手持机时拇指可触达。

### 5.4 视觉与文案要求

- 界面语言：**简体中文**（本期不做多语言）。
- 文案风格：直白、不卖萌、不用感叹号堆砌情绪；错误文案要说明「发生了什么 + 我能做什么」。
- 视觉基调：克制、工具感优先，不使用夸张渐变与炫技动效（避免 AI 生成味）。
- 占位符规则：所有未填信息用可读占位（如「未命名的记录」），不出现空白区域。

---

## 6. 数据字段

### 6.1 数据实体总览

| 表 | 实体 | 说明 |
|---|---|---|
| T-1 | `User` 用户 | 账号与登录状态 |
| T-2 | `Memory` 记忆条 | 核心内容实体（录音 / 文字） |
| T-3 | `Tag` 标签 | 用户自定义标签 |
| T-4 | `MemoryTag` 记忆-标签关联 | 多对多关联 |
| T-5 | `Setting` 设置项 | 用户级配置键值 |
| T-6 | `AppMeta` 应用元信息 | 数据版本、初始化标记 |
| T-7 | `SyncState` 同步状态（本地） | 每用户一行的同步游标与计数 |

### 6.2 T-1 User（用户）

| 字段 | 类型 | 必填 | 说明 | 约束 |
|---|---|---|---|---|
| `user_id` | 字符串 | ✅ | 用户唯一标识 | 主键，全局唯一 |
| `username` | 字符串 | ✅ | 登录账号 | 4–20 位；**唯一**；允许字母/数字/下划线 |
| `password_hash` | 字符串 | ✅ | 密码摘要 | **不得明文存储** |
| `nickname` | 字符串 | ❌ | 昵称 | 默认取 username |
| `status` | 枚举 | ✅ | 账号状态 | `active` / `disabled`，默认 `active` |
| `created_at` | 时间 | ✅ | 注册时间 | 写入后不可修改 |
| `last_login_at` | 时间 | ❌ | 最近登录时间 | 每次登录更新 |
| `is_logged_in` | 布尔 | ✅ | 当前是否保持登录 | 默认 `false`，用于免登录判断 |
| `server_user_id` | 字符串 | ✅ | 服务端返回的用户标识 | **由服务端分配**，是云同步的归属依据 |
| `access_token` | 字符串 | ❌ | 访问凭据（短期） | **存入系统安全存储**（不落普通库）；过期用 `refresh_token` 续期 |
| `refresh_token` | 字符串 | ❌ | 刷新凭据（长期） | 同上；退出登录时清除 |
| `token_expires_at` | 时间 | ❌ | 访问凭据到期时间 | 用于判断是否需刷新（对应异常 E15） |

### 6.3 T-2 Memory（记忆条）

| 字段 | 类型 | 必填 | 说明 | 约束 |
|---|---|---|---|---|
| `memory_id` | 字符串 | ✅ | 记忆唯一标识 | 主键 |
| `user_id` | 字符串 | ✅ | 归属用户 | 外键 → T-1，**数据隔离依据** |
| `type` | 枚举 | ✅ | 记录类型 | `audio` / `text` |
| `title` | 字符串 | ✅ | 标题 | 默认自动生成，用户可改；空值时回落为「未命名的记录」 |
| `text_content` | 文本 | ❌ | 文字内容 | `text` 型必填；`audio` 型为手动补充说明（可空） |
| `audio_path` | 字符串 | ❌ | 录音文件路径 | `audio` 型必填；`text` 型为空 |
| `audio_duration_ms` | 整数 | ❌ | 录音时长（毫秒） | `audio` 型必填；≥ 1000 |
| `audio_format` | 字符串 | ❌ | 音频格式 | 本期固定为无压缩 PCM WAV |
| `audio_size_bytes` | 整数 | ❌ | 文件大小（字节） | 用于计算占用空间 |
| `source` | 枚举 | ✅ | 记录来源 | `record`（录音）/ `quick_note`（速记），默认按入口写入 |
| `record_status` | 枚举 | ✅ | 记录完整状态 | `normal` / `incomplete`，默认 `normal`；录音被中断（来电 / 切后台）时存为 `incomplete`，界面标注「未完成」（对应异常 E2、验收 AC-I2 / AC-D2） |
| `is_deleted` | 布尔 | ✅ | 软删除标记 | 默认 `false`；删除时置 `true`（墓碑，见 6.6 第 4 条），音频文件移入 `trash/` 暂存区 |
| `deleted_at` | 时间 | ❌ | 删除时间 | 删除时写入；**作为同步「墓碑」**，让其他设备得知该条已被删。墓碑与音频文件解耦：文件先入 `trash/`，墓碑同步成功后再物理删除 |
| `created_at` | 时间 | ✅ | 创建时间 | **列表排序依据**，不可修改 |
| `updated_at` | 时间 | ✅ | 最后修改时间 | 编辑时更新 |
| `client_version` | 字符串 | ✅ | 写入时的 App 版本 | 便于后续数据迁移 |
| `sync_status` | 枚举 | ✅ | 云同步状态 | `local_only` / `pending` / `synced` / `failed`，默认 `pending`（对应 F-07、验收 K 组） |
| `server_version` | 整数 | ❌ | 服务端版本号 | 服务端每次写入自增，用于增量拉取与冲突判定 |
| `synced_at` | 时间 | ❌ | 最近一次同步成功时间 | 同步失败时保留上次成功值 |

### 6.4 T-3 Tag（标签）／ T-4 MemoryTag（关联）

**T-3 Tag**

| 字段 | 类型 | 必填 | 说明 | 约束 |
|---|---|---|---|---|
| `tag_id` | 字符串 | ✅ | 标签标识 | 主键 |
| `user_id` | 字符串 | ✅ | 归属用户 | 外键 → T-1 |
| `name` | 字符串 | ✅ | 标签名 | 1–12 位；**同一用户下唯一** |
| `color` | 字符串 | ❌ | 标签色 | 默认色；用于列表视觉区分 |
| `created_at` | 时间 | ✅ | 创建时间 | — |
| `is_deleted` | 布尔 | ✅ | 软删除标记 | 默认 `false` |

**T-4 MemoryTag**

| 字段 | 类型 | 必填 | 说明 | 约束 |
|---|---|---|---|---|
| `memory_id` | 字符串 | ✅ | 记忆标识 | 外键 → T-2 |
| `tag_id` | 字符串 | ✅ | 标签标识 | 外键 → T-3 |
| `created_at` | 时间 | ✅ | 关联时间 | — |

> 约束：`memory_id + tag_id` 组合唯一（同一条记忆不能重复挂同一标签）。

### 6.5 T-5 Setting（设置项）／ T-6 AppMeta（应用元信息）

**T-5 Setting**

| 字段 | 类型 | 必填 | 说明 | 约束 |
|---|---|---|---|---|
| `setting_id` | 字符串 | ✅ | 设置项标识 | 主键 |
| `user_id` | 字符串 | ✅ | 归属用户 | 外键 → T-1 |
| `key` | 字符串 | ✅ | 配置键 | 同一用户下唯一 |
| `value` | 字符串 | ✅ | 配置值 | 统一按字符串存储 |
| `updated_at` | 时间 | ✅ | 更新时间 | — |

**T-6 AppMeta**

| 字段 | 类型 | 必填 | 说明 | 约束 |
|---|---|---|---|---|
| `db_version` | 整数 | ✅ | 数据结构版本号 | 用于后续升级迁移 |
| `initialized_at` | 时间 | ✅ | 首次初始化时间 | 仅写入一次 |

**T-7 SyncState（同步状态，仅本地）**

| 字段 | 类型 | 必填 | 说明 | 约束 |
|---|---|---|---|---|
| `user_id` | 字符串 | ✅ | 归属用户 | 主键；每个登录用户一行 |
| `last_sync_at` | 时间 | ❌ | 最近一次同步成功时间 | 用于设置页展示与增量拉取游标 |
| `pull_cursor` | 字符串 | ❌ | 增量拉取游标 | 服务端返回，下次拉取带上 |
| `pending_count` | 整数 | ✅ | 待上传条数 | 实时维护，默认 `0` |
| `failed_count` | 整数 | ✅ | 同步失败条数 | 实时维护，默认 `0` |

### 6.6 通用字段规则（保证正常使用的基础要求）

1. **主键**：所有实体必须有稳定唯一标识，不得依赖自增序号（避免换设备 / 重装后标识冲突）。
2. **用户隔离**：所有业务数据必须带 `user_id`，查询时强制按当前登录用户过滤。
3. **时间字段**：`created_at` / `updated_at` 必须成对存在，写入自动生成，不允许为空。
4. **软删除（墓碑与音频文件解耦，两条独立生命线）**：默认查询过滤 `is_deleted = true` 的数据。删除分三步，其中**只有墓碑（数据库那一行）依赖网络，音频文件不依赖**：① 立即写 `is_deleted = true` + `deleted_at = now`（这行 = 墓碑，是同步凭据，保留到同步成功）；② 音频文件**移入** App 私有目录 `trash/` 暂存区（只移动、不物理删）；③ 该条墓碑**同步成功送达服务端后**，才从 `trash/` 物理删除音频文件。设置页「占用空间」须**计入 `trash/`**，并提供「清理已删除的音频」入口（避免磁盘幽灵占用，见 AC-K4）。
5. **数据版本**：`AppMeta.db_version` 存在且可读，为后续结构升级留出迁移依据。
6. **默认值**：所有可空字段必须有明确默认值，不允许出现「字段缺失导致页面空白」。
7. **同步字段**：所有需上云的实体必须带 `sync_status` 与 `updated_at`，作为增量同步与冲突判定的依据。
8. **标识一致性（关键）**：`memory_id` / `tag_id` 由**客户端生成 UUID**，服务端沿用同一主键 —— 避免「先本地、后同步」时发生 ID 重映射（这是幂等重试不产生重复条目的前提）。

### 6.7 数据安全与持久化要求

- 密码**不得明文存储**：客户端**不保存密码**；服务端只存**加盐哈希**（Argon2id 或 bcrypt）。
- 数据**本机为主副本、云端为同步与备份**：音频与元数据先落本机，联网后同步到服务端；**无网时全部记录动作照常可用**（验收见 K 组）。
- **传输必须加密**：客户端与服务端之间一律 **HTTPS / TLS**，不接受明文 HTTP。
- **凭据只存系统安全存储**：`access_token` / `refresh_token` 不得落普通数据库或明文文件。
- 应用被杀进程 / 设备重启后，数据与音频文件必须完整可读（验收见 H 组 AC-H1）。
- **严禁在提交到代码仓库的内容中包含任何密钥、账号密码、连接串**（项目规则第五章第 3 条）；服务端密钥一律走**环境变量**，不入库、不入镜像。

---

## 7. 异常状态

### 7.1 异常处理总表

| # | 场景 | 触发条件 | 期望表现 | 用户可做的恢复动作 |
|---|---|---|---|---|
| E1 | 麦克风权限被拒 | 首次录音时用户拒绝授权 | 不崩溃；弹出说明页，解释「为什么需要麦克风」 | 提供「去系统设置开启」入口；开启后可直接录音 |
| E2 | 录音过程被中断 | 来电 / 切后台 / 系统回收 | 已录部分**不丢失**，自动保存为一条记忆并标注「未完成」 | 回听已录部分；重新补录 |
| E3 | 存储空间不足 | 开始录音或保存时检测不足 | 不开始录音；提示「存储空间不足，请清理后重试」 | 清理空间后重试；或先做文字速记 |
| E4 | 音频文件丢失 / 损坏 | 文件被外部删除或读取失败 | 详情页不崩溃，播放区显示「音频文件不可用」 | 保留文字与标题，可手动删除该条 |
| E5 | 账号已存在 | 注册时用户名重复 | 提示「该账号已存在」，停留在注册页 | 换账号名或直接去登录 |
| E6 | 密码错误 / 账号不存在 | 登录校验失败 | 统一提示「账号或密码不正确」 | 重新输入；确认大小写 |
| E7 | 必填项为空 / 格式错误 | 提交表单时校验失败 | 在对应输入框下方就地提示，不弹窗 | 按提示修正后重提 |
| E8 | 速记内容为空 | 保存空文字 | 保存按钮不可点 / 提示「内容不能为空」 | 输入内容 |
| E9 | 录音短于 1 秒 | 误触松开 | 自动取消，提示「说话时间太短，已取消」 | 重新按住说话 |
| E10 | 音频读取/播放失败 | 播放器报错 | 停止播放，提示「播放失败，请重试」 | 重试；或删除该条 |
| E11 | 内存/磁盘写入失败 | 保存过程中写入异常 | 提示保存失败，**不生成半截数据**（事务性） | 重试保存 |
| E12 | 数据加载失败 | 列表查询异常 | 显示可读错误 + 「重试」按钮，不白屏 | 点击重试 |
| E13 | 网络不可用 | 记录 / 编辑 / 删除时无网 | **不阻断操作**：照常落本机，标 `pending`，界面给一次轻提示（不打断） | 继续使用；联网后自动补传 |
| E14 | 同步上传失败 | 上传超时 / 服务端报错 | 该条标 `failed`，**不丢本地数据**，**不产生重复条目** | 在 P-12 点「重试」，或等自动重试 |
| E15 | 登录凭据过期 | `access_token` 过期 / 被吊销 | 自动用 `refresh_token` 续期；续期失败则跳登录页（**本机数据保留**） | 重新登录（数据不丢） |
| E16 | 同一记忆多端冲突 | 两台设备先后修改同一条 | 按 `updated_at` 判定：**新者取胜**；被覆盖的一方**保留冲突副本**并在详情页标注 | 查看冲突副本，手动合并或删除 |
| E17 | 服务端 5xx / 维护中 | 接口返回 5xx | 提示「服务暂时不可用，稍后自动重试」；**本地功能不受影响** | 继续本地使用；稍后自动恢复 |

> **统一原则**：任何异常都不得导致 App 崩溃或白屏；任何异常都必须给出「发生了什么 + 我能做什么」两段式提示（见 5.4）。

### 7.2 空状态

| 位置 | 空状态表现 |
|---|---|
| 首页列表（新账号） | 引导文案：「还没有记录。按住下面按钮，说一句就开始。」+ 图示 + 引导操作 |
| 标签为空 | 在 P-07 编辑视图中显示「暂无标签，可新建」 |
| 设置页无数据 | 显示「0 条记录 · 0 MB」，不显示空白区域 |

### 7.3 提示文案规范

- 错误提示：`发生了什么` + `建议怎么做`（例：「存储空间不足，请清理后重试」）。
- 禁止只用「操作失败」「错误」「Error」这类无信息量文案。
- 破坏性操作弹窗必须包含「不可恢复」的明确表述。

---

## 8. 验收标准

> 本节回答当日核心要求的「可逐条检查」。**每一条都必须能用一个明确动作验证为「通过 / 不通过」**，不允许出现「体验良好」这类无法判定的表述。

### 8.1 使用说明

- 验收对象：**AI 记忆卡 · MVP（本地优先 + 云端同步）** —— 含客户端 App 与服务端两部分。
- 验收环境：真机运行（Android，**至少 2 台设备**用于验证多端同步），全新安装、空数据起步；**服务端已部署并可达**。
- 验收条目：**共 65 条**（P0 47 条 / P1 16 条 / P2 2 条），分 A–K 十一组。
- 判定方式：逐条执行，勾选通过；**任一条 P0 级不通过，则整体不通过**。

### 8.2 逐条验收清单

#### A 组 · 安装与启动（P0）

- [ ] **AC-A1** 全新安装后首次启动，App 能正常打开，无闪退、无白屏。（P0）
- [ ] **AC-A2** 启动流程符合 P-01 规则：未登录进登录页，已登录直达首页。（P0）
- [ ] **AC-A3** 杀掉进程后重新打开，仍保持上次的登录状态。（P0）

#### B 组 · 登录（F-01，P0）

- [ ] **AC-B1** 用新账号注册，注册成功后直接进入首页，列表为空并显示空状态文案。（P0）
- [ ] **AC-B2** 用已存在账号重复注册，提示「该账号已存在」，且未创建重复账号。（P0）
- [ ] **AC-B3** 账号 / 密码长度或字符不合法时，提交被阻止并就地提示原因。（P0）
- [ ] **AC-B4** 正确账号 + 正确密码，能成功登录并进入首页。（P0）
- [ ] **AC-B5** 正确账号 + 错误密码，提示「账号或密码不正确」（不区分具体错项）。（P0）
- [ ] **AC-B6** 账号 A 记录 2 条、账号 B 记录 1 条，切换登录后各自只能看到自己的记录。（P0）
- [ ] **AC-B7** 设置页退出登录需二次确认；确认后回到登录页，重启 App 仍为未登录。（P1）

#### C 组 · 快速记录（F-02，P0）

- [ ] **AC-C1** 按住主按钮即开始录音，录音中可见计时或动效，用户能确认「正在录」。（P0）
- [ ] **AC-C2** 松开手指后 1 秒内，新记忆条出现在列表最上方，类型标识为「录音」。（P0）
- [ ] **AC-C3** 录制一段 ≥ 5 秒的音频，结束后可正常回放，音频内容完整无截断。（P0）
- [ ] **AC-C4** 录音过程中上滑取消，列表不新增条目。（P0）
- [ ] **AC-C5** 按住不足 1 秒即松开，提示「说话时间太短，已取消」，且不生成条目、不残留文件。（P0）
- [ ] **AC-C6** 通过「速记」输入一行文字，保存后列表顶部生成类型为「文字」的条目。（P0）
- [ ] **AC-C7** 速记内容为空时无法保存，并给出提示。（P0）
- [ ] **AC-C8** 新条目自动生成形如「09-20 20:31 的记录」的默认标题，非空白、不含「null」等占位字符。（P1）
- [ ] **AC-C9** 单条录音达到 30 分钟上限时，自动结束录音、保存为一条记录，并给出明确提示。（P1）

#### D 组 · 记忆列表（F-03，P0）

- [ ] **AC-D1** 列表按创建时间**倒序**排列，最新一条在最上方。（P0）
- [ ] **AC-D2** 每条至少显示：标题、类型标识、创建时间；录音条额外显示时长；`incomplete` 条目显示「未完成」标识。（P0）
- [ ] **AC-D3** 点击任意条目，能进入对应详情页，且内容与该条一致（不错位）。（P0）
- [ ] **AC-D4** 长按条目弹出快捷操作菜单，含「重命名」「删除」。（P1）
- [ ] **AC-D5** 在列表内点播放按钮可就地试听；播放另一条时，前一条自动停止。（P1）
- [ ] **AC-D6** 列表为空时显示空状态引导，而非空白页。（P0）
- [ ] **AC-D7** 累积 100 条记录时，列表滚动流畅、无肉眼可见卡顿。（P1）
- [ ] **AC-D8** 删除某条后，列表立即刷新且该条不再出现；重启 App 后依然不出现。（P0）

#### E 组 · 记忆详情（F-04，P0）

- [ ] **AC-E1** 详情页正确展示标题、类型、创建时间、时长（录音）、文字内容、标签。（P0）
- [ ] **AC-E2** 录音可播放 / 暂停；播放中进度条与时间同步推进。（P0）
- [ ] **AC-E3** 可拖动进度条定位到任意位置播放。（P0）
- [ ] **AC-E4** 播放到结尾后自动停止，按钮状态回到「播放」。（P1）
- [ ] **AC-E5** 在详情页修改标题并保存，返回列表后列表标题同步更新。（P0）
- [ ] **AC-E6** 音频文件被外部删除后进入该详情页，页面不崩溃并提示「音频文件不可用」。（P1）

#### F 组 · 基础管理（F-05，P1）

- [ ] **AC-F1** 可修改标题，标题修改后列表与详情页显示一致。（P0）
- [ ] **AC-F2** 可给记忆条添加标签，也可移除标签。（P1）
- [ ] **AC-F3** 可新建标签，同一用户下重复标签名不允许创建。（P1）
- [ ] **AC-F4** 删除任何记忆**都必须**弹出二次确认，弹窗中含「删除后不可恢复」表述。（P0）
- [ ] **AC-F5** 在确认弹窗中选「取消」，条目、文字、音频文件均无任何变化。（P0）
- [ ] **AC-F6** 确认删除后，该条的本地音频文件被一并清理（不留孤儿文件）。（P1）

#### G 组 · 设置（F-06，P2）

- [ ] **AC-G1** 设置页显示当前账号名、记忆条数量、占用空间（约值）。（P2）
- [ ] **AC-G2** 设置页可查看版本号与关于信息。（P2）

#### H 组 · 数据与持久化（P0）

- [ ] **AC-H1** 记录若干条后强制杀掉 App 再打开，所有记录（含录音文件）完整存在。（P0）
- [ ] **AC-H2** 客户端**不以明文保存密码**（客户端存储内无明文密码）；服务端只保存加盐哈希（与 AC-K10 同源验证）。（P0）
- [ ] **AC-H3** 所有业务数据带用户归属字段，跨账号不可见（与 AC-B6 同源验证）。（P0）
- [ ] **AC-H4** 数据版本号字段存在且可读，供后续结构升级使用。（P1）
- [ ] **AC-H5** 代码仓库中不存在任何密钥、账号密码、连接串（提交前检查）。（P0）

#### I 组 · 异常与健壮性（P0）

- [ ] **AC-I1** 拒绝麦克风权限后仍可进入 App 并使用文字速记；再次录音时有明确引导。（P0）
- [ ] **AC-I2** 录音过程中切后台或接来电，返回后已录内容不丢失，至少保存为一条「未完成」记录。（P0）
- [ ] **AC-I3** 存储空间不足时给出明确提示，且不产生半截损坏数据。（P0）
- [ ] **AC-I4** 遍历 7.1 表中 **E1–E17** 全部异常场景，逐项确认**无闪退、无白屏**。（P0）

#### J 组 · 体验与性能底线（P1）

- [ ] **AC-J1** 从松手到列表出现新条目，耗时 ≤ 1 秒。（P0）
- [ ] **AC-J2** 冷启动到首页可交互，耗时 ≤ 3 秒。（P1）
- [ ] **AC-J3** 按住录音的主按钮在单手持机时可被拇指触达，且不与系统手势冲突。（P1）
- [ ] **AC-J4** 界面文案为简体中文，无乱码、无未翻译占位符、无 Lorem 假文。（P0）
- [ ] **AC-J5** 全流程无「点了没反应」的按钮（每个可交互元素都有可见反馈）。（P1）

#### K 组 · 云同步（F-07，P0）

- [ ] **AC-K1** 断网状态下按住说话保存成功，条目出现在列表，同步状态标为 `pending`。（P0）
- [ ] **AC-K2** 网络恢复后，`pending` 条目在设定时间内**自动**变为 `synced`，无需用户手动操作。（P0）
- [ ] **AC-K3** 在设备 A 新增一条记忆，设备 B 登录同一账号后能拉到该条，内容一致（含音频可回放）。（P0）
- [ ] **AC-K4** 在设备 A 删除一条记忆，设备 B 同步后该条也消失。（P0）
- [ ] **AC-K5** 同步后的录音在设备 B 上能完整回放，时长与设备 A 一致。（P0）
- [ ] **AC-K6** 上传失败条目标为 `failed`；点「重试」后能成功同步，且**不产生重复条目**。（P0）
- [ ] **AC-K7** 列表项 / 详情页 / 设置页能看到单条或整体的同步状态。（P1）
- [ ] **AC-K8** `access_token` 过期后自动续期、用户无感知；续期失败跳登录页且**本机数据保留**。（P0）
- [ ] **AC-K9** 客户端与服务端通信**全程 HTTPS**，抓包不存在明文 HTTP 请求。（P0）
- [ ] **AC-K10** 客户端存储中不存在明文密码；服务端只存加盐哈希。（P0）

### 8.3 判定规则

| 规则 | 说明 |
|---|---|
| **整体通过条件** | 全部 **P0** 条（47 条）通过，且 P1 条通过率 ≥ 80%（16 条中至少 13 条通过）；P2 条（2 条）不作门槛，仅记录问题 |
| **不通过处理** | 任一 P0 条不通过 → 记为「必须修改」级问题，修复后重新验收该条 |
| **不可判定处理** | 若某条无法用明确动作判定 → 该条视为验收标准写得不合格，需重写 |
| **回归要求** | 修复 P0 问题后，同组已通过的条目需重新抽查，确认未被破坏 |

---

## 附录 A · 待确认事项

| # | 事项 | 当前状态 | 影响 |
|---|---|---|---|
| A1 | 转写供应商（讯飞 / 腾讯） | ⏳ **挂起未定** | **不影响本期验收**（AI 接入本期不做）；仅在后续立项 AI 功能时才需要拍板 |
| A2 | 「按住说话」是否需要在录音时同时显示波形 | 待定 | 影响 P-05 的实现成本，属体验优化项 |
| A3 | 默认标题命名规则 | ✅ **已定**：`MM-DD HH:mm 的记录`（如「09-20 20:31 的记录」） | AC-C8 据此判定 |
| A4 | 标签是否需要「预置标签」 | 待定 | 影响 P-07 的交互细节 |
| A5 | 是否需要「回收站」 | 本期**不做**（删除即永久，靠二次确认兜底） | 若未来改为要做，F-05 的删除逻辑需重做，属独立需求 |
| A6 | 服务端部署环境（云服务器 / 容器平台） | 待定 | 影响 F-07 的上线方式与成本，**不影响客户端设计** |
| A7 | 音频存储位置（服务端本地磁盘 / 对象存储） | 待定 | 影响 F-07 的上传方式与成本；MVP 可先落服务端磁盘 |
| A8 | 同步冲突策略是否够用（最近修改取胜 + 保留副本） | 待验证 | 若真实使用中冲突频繁，需升级为字段级合并，属独立需求 |
| A9 | 登录态可离线保持多久 | 待定 | 影响 F-01 的离线可用体验与 E15 的触发频率 |

## 附录 B · 与 `research.md` 的差异说明（诚实记录）

| # | 差异点 | `research.md` 原表述 | 本 PRD 表述 | 原因 |
|---|---|---|---|---|
| B1 | MVP 的功能构成 | 28 天 MVP 为「第 1 周录音+列表 / 第 2 周转写 / 第 3 周 AI 整理 / 第 4 周打通口语」 | MVP 只做**基础记录闭环**（登录 + 记录 + 列表 + 详情 + 管理），AI 接入**本期不交付、不验收** | 用户 2026-09-20 口径调整：「AI 接入保留但先做基础功能」（详见 `.workbuddy/memory/2026-09-20.md`） |
| B2 | 账号体系（v0.2 口径） | 「账号体系 + 云同步」列入本期不做 | ~~登录保留（轻量本地账号），云同步仍不做~~ → **已被 B5 / B6 取代** | 记录 v0.2 当时口径；**最新口径见 B4 ~ B6** |
| B3 | 页面数量 | 未明确 | 明确为本期 **12 个**页面 / 视图（含 1 个通用二次确认弹窗 + 1 个同步状态视图） | 本 PRD 需要可验收的页面清单 |
| B4 | 后端 | 「账号体系 + 云同步」列入本期不做 | **本期实现**：账号走服务端、记忆上云多端同步（F-07） | 用户 2026-09-20 晚的**主动范围扩张**，见附录 C.6 |
| B5 | 账号形态 | 轻量**本地**账号 | **服务端**账号（客户端不存明文密码） | 同上；后端建立后本地账号已无必要 |
| B6 | 数据存哪 | 「音频与数据存在**本机**；本期不上传云端、不做任何网络同步」 | 本机为主副本 + **服务端同步与备份** | 同上 |

> **说明**：B1/B2 属**有意调整**，非笔误；B4/B5/B6 是用户 2026-09-20 晚的**主动范围扩张**，同样属有意调整、非笔误。`research.md` 第 6 节尚未同步这批口径 —— 是否回改 `research.md`，由用户决定。

---

## 附录 C · AI 自检记录（2026-09-20）

### C.1 自检方式

按第 8 章验收标准的口径，对 **PRD 文档本身**做四项交叉核对（不是核对 App 实现）：

1. **结构完整性**：用户指定的 8 节是否齐全，每节是否有实质内容。
2. **功能描述完备性**：每个功能是否都写了「用户动作 + 可观察结果」。
3. **内部一致性**：功能约束 / 数据字段 / 异常表 / 验收条之间是否互相矛盾或缺失。
4. **可判定性**：每条验收标准能否用一个明确动作判定「通过 / 不通过」。

### C.2 发现的问题与处理

**必须修改级（会导致开发歧义或验收无法判定，已全部修复）**

| # | 问题 | 位置 | 处理 |
|---|---|---|---|
| M1 | 验收要求保存「未完成」记录，但数据模型**没有承载该状态的字段** | 7.1 E2 / AC-I2 ↔ 6.3 T-2 | 在 `T-2 Memory` 新增 `record_status` 字段（`normal` / `incomplete`），并在 F-02、F-03 同步说明 |
| M2 | 功能 F-05 标 **P1**，但其下 AC-F1 / F4 / F5 标 **P0** —— 优先级冲突，会导致判定规则失效 | 3.1 / 3.6 ↔ F 组 | 将 F-05 整体上调为 **P0**，标签子功能明确为 P1（与 AC-F2 / F3 / F6 对齐） |
| M3 | F-02 规定「最长 30 分钟自动结束」，但 C 组**没有对应验收条** —— 规则无验收覆盖 | 3.3 ↔ C 组 | 新增 **AC-C9** |
| M4 | 8.2 节标注「约 45 条」，与实际条数不符 —— 文档数字不准 | 8.1 | 实测为 **55 条**（P0 38 / P1 15 / P2 2），已改为准确值并写入 8.1 |

**建议修改级（不影响判定，已顺手修复）**

| # | 问题 | 处理 |
|---|---|---|
| S1 | 5.1 节说明只有 P-05 / P-08 / P-10 是视图，漏了 P-07 | 补齐为 P-05 / P-07 / P-08 / P-10，并列出 6 个独立页面 |
| S2 | 异常 E9 首列文字为「声短于 1 秒」，表述不完整 | 改为「录音短于 1 秒」 |
| S3 | AC-C8 的判定依赖附录 A3（默认标题规则待定），存在不可判定风险 | 定死规则为 `MM-DD HH:mm 的记录`，附录 A3 由「待定」改为「已定」 |
| S4 | 8.3 判定规则中「P1/P2 通过率 ≥ 80%」表述模糊 | 明确为「P1 15 条中至少 12 条通过；P2 仅记录，不作门槛」 |

### C.3 复检结论

对上述 8 项修复逐项复查后：

- **结构完整性**：8 节 + 3 附录齐全，无空节。**通过**
- **功能描述完备性**：F-01 ~ F-06 全部具备「用户故事 / 用户动作 / 可观察结果 / 边界说明」。**通过**
- **内部一致性**：功能约束 ↔ 数据字段 ↔ 异常表 ↔ 验收条四者已互相咬合（`record_status` 一处修好，贯穿 E2 / AC-I2 / AC-D2）。**通过**
- **可判定性**：55 条验收标准全部可用明确动作判定，无「体验良好」式表述。**通过**

> **自检结论：无遗留「必须修改」级问题。**

### C.4 v0.3 修订（2026-09-20 第二轮，用户追加要求）

| 要求 | 处理 |
|---|---|
| ① 删除「以后再说但没有边界」的空泛表述 | 全文扫出 **16 处**，逐处改写为「**本期不做 + 明确理由 + 是否纳入验收**」。典型替换：`列为后续` → `本期不做（理由…，见 4.1）`；`可预见版本内不做` → `本 PRD 有效期内明确不做`；`属于产品第二阶段的形态` → `本期无数据基础（真实记录量为 0，无法验证）`。另修正 1 处**自相矛盾**表述（F-06 原「不做『一键清空全部数据』以外的批量清理」）。并在 4.1 节**新增边界声明**：4.1 的功能一律不实现、不验收，本 PRD 的验收范围只含 F-01 ~ F-06。 |
| ② 检查每个 MVP 功能能否拆成 ≤1 天的小任务 | 新增 **附录 D**，逐功能拆出 33 个子任务并标注粒度。结论：6 个功能**全部可拆**；唯一超粒度项为「录音中断恢复」（1.5–2 天），已给出降级方案。 |

> 本轮修订后附录总数由 3 个增至 **4 个**（A / B / C / D）。

### C.5 v0.4 修订（三项结构检查，2026-09-20 第三轮）

| # | 检查项 | 检查方式 | 结论 | 处理 |
|---|---|---|---|---|
| 1 | **随机挑一个功能，能否回答「谁 / 在什么页面 / 做什么 / 看到什么」** | 系统随机抽中 **F-05 基础管理**（另抽 F-04 复核），再全量复查其余 4 个 | ⚠️ **部分通过**：「谁」（用户故事）、「做什么」（用户动作）、「看到什么」（可观察结果）三项**原有**；「**在什么页面**」原本**隐藏在动作描述里，未显式标注** —— 读者需自己从「在详情页或列表长按菜单中」反推页面编号 | 为第 3 章 **6 个功能各新增一行「涉及页面」**，显式列出 P-xx 编号（F-05 现为 P-07 / P-06 / P-04 / P-11） |
| 2 | **PRD 是否有 MVP 与后续的分界** | 查分界是否可判定、是否有越界表述 | ✅ **通过**：① 1.2 节两线状态表；② 第 3 章「MVP 功能」（F-01~F-06）↔ 第 4 章「后续功能与本期砍掉的功能」；③ 4.1 顶部**边界声明**明确「4.1 一律不实现、不验收，验收范围只含 F-01~F-06」 | 无需修改 |
| 3 | **是否有至少三个页面 / 视图的初步规划** | 数 5.1 节页面清单，检查是否含「入口 / 内容 / 交互」 | ✅ **通过（超额）**：共 **11 个**页面 / 视图，每个都写明入口、核心内容、关键交互 | 无需修改（本轮由 10 个增至 11 个，见下） |

**本轮核查顺带发现并修复的 2 个真错误**（均为「交叉引用指错」，会直接导致找不到、判不了）

| # | 问题 | 位置 | 处理 |
|---|---|---|---|
| 1 | 「二次确认弹窗」被 **AC-F4** 作为可验收对象点名，但在 5.1 页面清单里**没有编号** —— 属「被验收却没有身份」 | 3.6 ↔ 5.1 | 补为 **P-11**，同步更新 5.1 合计数字（10 → 11）与附录 B3 |
| 2 | **引用了不存在的验收组**：正文写「详见 **AC-P** 组」「持久化验证见 **AC-DATA** 组」，而验收标准只有 A–J 十组 —— **P 组与 DATA 组根本不存在**，读者按此查找会落空，等同于该条不可判定 | 3.4 可观察结果、6.7 数据安全与持久化要求 | 分别改指真实条号：**AC-D7**（列表 100 条性能）、**H 组 AC-H1**（持久化） |

> 错误 #2 特别值得记住：**「引用了一个不存在的编号」比「写错内容」更隐蔽** —— 通读时看起来完全正常，只有做交叉引用检查才会暴露。本轮做法是把全文 `AC-xx` 引用序列与实际定义序列做机器比对，一次性扫净。

### C.6 v0.5 修订（2026-09-20 第四轮：**MVP 加入后端与云同步**）

**变更起因**：用户决定「加后端」，并选择「**后端正式进 MVP** + **Python / FastAPI** + **同步改 PRD**」。这是**主动范围扩张**，不是纠错。

**本次变更清单**

| # | 位置 | 改动 |
|---|---|---|
| 1 | 1.2 / 1.3 | 形态由「纯本地 App」改为「**客户端 + 服务端**」；MVP 目标由「本地闭环」改为「**本地优先 + 云端同步**」 |
| 2 | 1.4 | 新增名词：本地优先、云同步、同步状态 |
| 3 | 2.3 / 3.1 / 3.8 | 新增功能 **F-07 云同步（P0）**，含完整描述（3.8 节） |
| 4 | 3.2 F-01 | 账号由「本地账号」改为「**服务端账号**」，密码只在服务端哈希存储 |
| 5 | 3.7 F-06 | 设置页新增**同步状态**展示与手动同步入口 |
| 6 | 4.1 | **移出**「云同步与多端」（改列本期功能）；**新增**「找回密码」为后续；转写行补注「服务端已就绪」 |
| 7 | 4.2 C7 | 由「云账号体系」**收窄**为「账号增强（短信验证码 / 第三方登录）」—— 账号与云同步本身已进本期 |
| 8 | 5.1 / 5.2 | 新增 **P-12 同步状态视图**（合计 11 → 12）；新增「流程 4 · 云同步」 |
| 9 | 6.2 / 6.3 | T-1 新增 `server_user_id` 与 token 字段；T-2 新增 `sync_status` / `server_version` / `synced_at` / `deleted_at` |
| 10 | 6.1 / 6.5 | 新增 **T-7 SyncState**（同步游标与计数） |
| 11 | 6.6 / 6.7 | 新增同步字段与**标识一致性**规则；数据安全要求加入 HTTPS、凭据安全存储、服务端哈希 |
| 12 | 7.1 | 新增 **E13 ~ E17** 五条网络 / 同步异常 |
| 13 | 8 | 新增 **K 组 · 云同步（10 条）**；总数 55 → **65 条**（P0 47 / P1 16 / P2 2） |
| 14 | 附录 A / B | 新增 A6 ~ A9 四项后端待定；新增 B4 ~ B6 三条差异说明 |

**风险如实记录（不掩盖）**

- **工期**：MVP 由「≈22 人日 ≈ 4.5 周」升至 ≈ **34–36 人日 ≈ 7 周**（重估明细见附录 D.2.1），**明显超出 28 天目标**。
- **复杂度**：同步引入了**离线队列、失败重试、冲突处理**三类新问题，是本期的最大技术风险点（详见 `TECH_DESIGN.md` 第 5、6 章）。

---

## 附录 D · MVP 功能「一天粒度」拆解检查

> **检查标准**：拆解到「一个人、一个工作日能完成并自我验证」的粒度。
> **判据**：拆不出 ≤1 天粒度的功能 = 功能描述太粗，需要细化或拆功能。

### D.1 逐功能拆解

**F-01 用户登录**（合计 ≈ 3.25 人日 / 6 个任务 → ✅ 全部 ≤1 天）

| # | 子任务 | 粒度 | 对应验收 |
|---|---|---|---|
| D1-1 | 登录页 / 注册页 UI（P-02、P-03） | 1 天 | AC-B1 |
| D1-2 | 表单校验（长度 / 字符 / 必填 / 重复账号） | 0.5 天 | AC-B2、AC-B3 |
| D1-3 | 用户表建表 + 注册写入 + 密码摘要存储 | 0.5 天 | AC-B1、AC-H2 |
| D1-4 | 登录校验 + 登录态保持 | 0.5 天 | AC-A2、AC-A3、AC-B4、AC-B5 |
| D1-5 | 退出登录 + 二次确认 | 0.25 天 | AC-B7 |
| D1-6 | 数据按 `user_id` 隔离 | 0.5 天 | AC-B6、AC-H3 |

**F-02 快速记录**（合计 ≈ 5.25–5.75 人日 / 9 个任务 → ⚠️ 1 项超粒度）

| # | 子任务 | 粒度 | 对应验收 |
|---|---|---|---|
| D2-1 | 首页主按钮 + 按住 / 松开 / 上滑取消手势 | 1 天 | AC-C1、AC-C4 |
| D2-2 | 录音能力接入（WAV PCM16 落盘） | 1 天 | AC-C3 |
| D2-3 | 录音中视图 P-05（计时 + 动效） | 0.5 天 | AC-C1 |
| D2-4 | 麦克风权限申请与拒绝引导（E1） | 0.5 天 | AC-I1 |
| D2-5 | 保存写库 + 列表回流（≤1 秒） | 0.5 天 | AC-C2、AC-J1 |
| D2-6 | 边界处理：<1 秒取消 / 30 分钟上限 | 0.5 天 | AC-C5、AC-C9 |
| D2-7 | 速记弹层 P-08 + 空内容校验 | 0.5 天 | AC-C6、AC-C7 |
| D2-8 | 默认标题生成规则 | 0.25 天 | AC-C8 |
| **D2-9** | **录音中断恢复（E2：切后台 / 来电时保留已录部分）** | **⚠️ 1.5–2 天** | AC-I2 |

**F-03 记忆列表**（合计 ≈ 3.5 人日 / 7 个任务 → ✅）

| # | 子任务 | 粒度 | 对应验收 |
|---|---|---|---|
| D3-1 | 列表 UI + 倒序查询 | 0.5 天 | AC-D1、AC-D2 |
| D3-2 | 列表项元素（类型 / 时长 / 未完成标识） | 0.5 天 | AC-D2 |
| D3-3 | 空状态 P-10 | 0.25 天 | AC-D6 |
| D3-4 | 长按菜单（重命名 / 删除） | 0.5 天 | AC-D4 |
| D3-5 | 就地试听 + 单例播放控制 | 1 天 | AC-D5 |
| D3-6 | 点击进详情 + 数据不错位 | 0.25 天 | AC-D3 |
| D3-7 | 100 条列表性能验证 | 0.5 天 | AC-D7 |

**F-04 记忆详情**（合计 ≈ 2.5 人日 / 5 个任务 → ✅）

| # | 子任务 | 粒度 | 对应验收 |
|---|---|---|---|
| D4-1 | 详情页布局 + 字段展示 | 0.5 天 | AC-E1 |
| D4-2 | 播放器：播放 / 暂停 / 进度拖动 | 1 天 | AC-E2、AC-E3 |
| D4-3 | 播放结束复位 | 0.25 天 | AC-E4 |
| D4-4 | 编辑标题 + 列表同步 | 0.5 天 | AC-E5、AC-F1 |
| D4-5 | 音频缺失异常处理（E4） | 0.25 天 | AC-E6 |

**F-05 基础管理**（合计 ≈ 2.25 人日 / 4 个任务 → ✅）

| # | 子任务 | 粒度 | 对应验收 |
|---|---|---|---|
| D5-1 | 编辑视图 P-07（标题 + 文字说明） | 0.5 天 | AC-F1 |
| D5-2 | 删除 + 二次确认弹窗 + 文件清理 | 0.5 天 | AC-F4、AC-F5、AC-F6 |
| D5-3 | 软删除过滤逻辑 | 0.25 天 | AC-D8 |
| D5-4 | 标签：T-3 / T-4 表 + 新建 / 添加 / 移除 | 1 天 | AC-F2、AC-F3 |

**F-06 设置**（合计 ≈ 0.75 人日 / 2 个任务 → ✅）

| # | 子任务 | 粒度 | 对应验收 |
|---|---|---|---|
| D6-1 | 设置页 P-09 + 统计（条数 / 占用空间） | 0.5 天 | AC-G1 |
| D6-2 | 版本 / 关于信息 | 0.25 天 | AC-G2 |

**跨功能横向任务**（不属于单个功能，但必须计入排期）

| # | 子任务 | 粒度 | 对应验收 |
|---|---|---|---|
| D0-1 | 数据库建库 + 版本字段（T-1 ~ T-6） | 1 天 | AC-H1、AC-H3、AC-H4 |
| D0-2 | 应用骨架 + 启动流程 P-01 | 1 天 | AC-A1、AC-A2 |
| D0-3 | 异常统一处理（E1–E12 遍历） | 1 天 | AC-I4 |
| D0-4 | 真机回归，第 8 章验收清单逐条过 | 1 天 | AC-* 全部 |

### D.2 检查结论

| 项 | 结论 |
|---|---|
| **可拆解性** | **6 个功能全部可拆成 ≤1 天的任务**（33 个子任务），没有出现「拆不动」的过粗功能 ✅ |
| **唯一超粒度项** | **D2-9 录音中断恢复**（1.5–2 天）：涉及音频会话与 App 生命周期，压不到 1 天。**建议降级**：基础版先保证「崩溃后不留损坏数据」（1 天），完整恢复能力单独立项 |
| **总工作量（v0.5，含后端）** | 客户端 ≈ 17.5–18 人日 + 横向 4 人日 **+ 服务端与云同步 ≈ 12–14 人日**（见 D.2.1）≈ **34–36 人日**；单人开发、每周 5 个有效工作日 ≈ **7 周**（**明显超出 28 天目标**，需靠砍功能或加人才能压回 4 周内） |
| **建议推进顺序** | 先 D0-1 / D0-2 打地基 → **先做服务端最小可用（账号 + 记忆同步接口）** → 再按 **F-02 → F-03 → F-04 → F-01 → F-05 → F-07 → F-06** 推进（先打通「记录 → 看到 → 回听」核心链路，登录与同步随后接入）|

**D.2.1 服务端与云同步拆解（v0.5 新增）**

| # | 子任务 | 粒度 | 对应验收 |
|---|---|---|---|
| S1-1 | 服务端骨架 + 数据库建表（users / memories / tags / memory_tags） | 1 天 | — |
| S1-2 | 注册 / 登录接口 + 密码哈希 + JWT 签发 | 1 天 | AC-B1~B5、AC-K10 |
| S1-3 | 凭据刷新接口 + 鉴权中间件 | 1 天 | AC-K8、AC-K9 |
| S1-4 | 记忆 CRUD 接口（含增量拉取 `since` 游标） | 1.5 天 | AC-D1、AC-K3 |
| S1-5 | 音频上传 / 下载接口 | 1 天 | AC-K5 |
| S1-6 | 客户端同步引擎（待上传队列 + 状态机） | 2 天 | AC-K1、AC-K2、AC-K7 |
| S1-7 | 失败重试与幂等（防重复条目） | 1 天 | AC-K6 |
| S1-8 | 删除墓碑同步 | 0.5 天 | AC-K4 |
| S1-9 | 冲突处理（最近修改取胜 + 保留副本） | 1 天 | 异常 E16 |
| S1-10 | 服务端部署 + 环境变量与密钥管理 | 1 天 | AC-K9 |
| S1-11 | 端到端联调（双设备同步回归） | 1 天 | K 组全部 |

> ⚠️ 该拆解为**相对粒度估算**，用于判断能否拆到一天内。服务端工作与客户端**可并行**：若两人同时开工，日历工期可压回约 **5 周**。

> **说明**：D.1 的粒度估算是**相对粒度**（用于判断能否拆到一天内），不是承诺工期；实际耗时受技术选型（Day 5）影响。

---

**文档结束** · 下一步：列出改动文件清单、提交并推送（Day 5 收尾）。
